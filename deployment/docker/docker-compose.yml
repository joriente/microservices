version: '3.8'

# MongoDB, RabbitMQ, and PostgreSQL are now managed by .NET Aspire
# This docker-compose file is kept for reference but containers are created by Aspire
# If you want to run services without Aspire, uncomment the services below

services:
  # MongoDB - DISABLED (Aspire manages this)
  # mongodb:
  #   image: mongo:7
  #   container_name: ProductOrdering-mongodb-legacy
  #   restart: unless-stopped
  #   ports:
  #     - "27017:27017"
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: admin
  #     MONGO_INITDB_ROOT_PASSWORD: admin123
  #   volumes:
  #     - mongodb_data:/data/db
  #     - mongodb_config:/data/configdb
  #   networks:
  #     - product-ordering-network

  # RabbitMQ and PostgreSQL are also managed by Aspire
  # This file can be deleted or kept for non-Aspire deployments
  
  placeholder:
    image: hello-world
    container_name: ProductOrdering-placeholder

volumes:
  mongodb_data:
    driver: local
  mongodb_config:
    driver: local
  seq_data:
    driver: local

networks:
  product-ordering-network:
    driver: bridge
